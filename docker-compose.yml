services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: captcha-solver-app
    environment:
      API_PORT: ${API_PORT:-3000}
    ports:
      - "${API_PORT:-3000}:${API_PORT:-3000}"
    restart: unless-stopped